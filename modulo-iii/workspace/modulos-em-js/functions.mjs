export function showAge(name, age) {
    return `A idade de ${name} é ${age}.`;
}

function showTown(name, town) {
    return `A cidade de ${name} é ${town}.`;
}

function showHobby(name, hobby) {
    return `O hobby de ${name} é ${hobby}.`;
}

export {
    showTown,
    showHobby
};